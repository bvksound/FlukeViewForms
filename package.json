{
  "name": "flukeview-forms-open",
  "version": "0.1.0",
  "description": "Open-source, browser-based data viewer/logger for the Fluke 287 multimeter (IR serial port).",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node --test test/*.test.js"
  }
}
